// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDF3e8qk8M6izEKaGvA5Ed9PK_jtoRMaA4",
    authDomain:  "recordando-la-5f62a.firebaseapp.com",
    databaseURL:  "https://recordando-la-5f62a.firebaseio.com",
    projectId: "recordando-la-5f62a",
    storageBucket: "recordando-la-5f62a.appspot.com",
    messagingSenderId: "344047718350",
  }
};